<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="play">

	<select id = "count" parameterType="play.PlayVO" resultType="Integer">
		SELECT COUNT(*) FROM play
		<where>
			<if test="sval != null and sval != ''">
			playName LIKE '%${sval}%'
			OR no IN
			(SELECT no FROM play WHERE content LIKE '%${sval}%')
			</if>
		</where>
	</select>
	
	<select id = "list" parameterType="play.PlayVO" resultType="play.PlayVO">
		SELECT * FROM play 
		<where>
			<if test="sval != null and sval != ''">
			playName LIKE '%${sval}%'
			OR no IN
			(SELECT no FROM play WHERE content LIKE '%${sval}%')
			OR no IN
			(SELECT no FROM play WHERE actor LIKE '%${sval}%')
			</if>
		</where>
		ORDER BY regDate DESC LIMIT #{startRow}, #{size}
	</select>
	
	<insert id = "write" parameterType="play.PlayVO" useGeneratedKeys="true">
		INSERT INTO play
			(playName, startDate, endDate, hallNo, actor, content,
			fileName, priceA, priceB, priceC, exhPrice, regDate, modDate)
		VALUES
			(#{playName}, #{startDate}, #{endDate}, #{hallNo},
			#{actor}, #{content}, #{fileName}, #{priceA}, #{priceB},
			#{priceC}, #{exhPrice}, now(), now());
		<selectKey keyProperty="no" resultType="Integer" order="AFTER">
			SELECT last_insert_id();
		</selectKey>
	</insert>
	
</mapper>