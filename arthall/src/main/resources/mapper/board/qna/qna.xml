<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qna">

	<select id="count" parameterType="board.qna.QnaVO" resultType="Integer">

		SELECT COUNT(*) FROM qna
	 	<where>
            <if test="sval != null and sval !='' and stype != 'all'" >
                ${stype} LIKE '%${sval}%'
            </if>
            <if test="sval != null and sval !='' and stype == 'all'">
            	title LIKE '%${sval}%'
            	or no in 
				(SELECT no FROM qna WHERE content LIKE '%${sval}%')
            </if>
         </where>
	</select>
	<select id="list" parameterType="board.qna.QnaVO" resultType="board.qna.QnaVO">
		
		SELECT * FROM qna
		<where>
         <if test="sval != null and sval !='' and stype != 'all'">
             ${stype} like '%${sval}%'
         </if>
         <if test="sval != null and sval !='' and stype == 'all'">
            	title LIKE '%${sval}%'
            	or no in 
				(SELECT no FROM qna WHERE content LIKE '%${sval}%')
          </if>
      	</where>
	 	ORDER BY no DESC LIMIT #{startRow}, #{size}
	</select>
	
	<!-- 20200724 추가 -->
	<insert id="write" parameterType="board.qna.QnaVO" useGeneratedKeys="true">
		INSERT INTO faq
			(writer, title, content , regdate, moddate, filename)
		VALUES
			(#{writer}, #{title}, #{content}, now(), now(), #{filename})
			 
		<selectKey keyProperty="no" resultType="Integer" order="AFTER">
			SELECT last_insert_id()
		</selectKey>
	</insert>
	
	<select id="view" parameterType="board.qna.QnaVO" resultType="board.qna.QnaVO">
		SELECT
			no, writer, title, content, regdate, filename
		FROM faq
		where no=#{no}
	</select>
	
	<update id="modify" parameterType="board.qna.QnaVO">
		UPDATE faq SET
			title = #{title} , content = #{content}, moddate = now(), filename=#{filename}
		WHERE no=#{no}	
	</update>
	
	
	<update id="delete" parameterType="Integer">
		DELETE FROM faq
		WHERE no=#{no}
	</update>
	
</mapper>